import { EB_Garamond, Satisfy } from "next/font/google";

export const garamond = EB_Garamond({
  weight: "400",
  subsets: ["latin"],
  variable: "--font-garamond",
});

export const satisfy = Satisfy({
  weight: "400",
  subsets: ["latin"],
  variable: "--font-satisfy",
});
